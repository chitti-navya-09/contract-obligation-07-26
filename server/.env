# Replace 'YOUR_PASSWORD_HERE' with your actual PostgreSQL password for the 'postgres' user.
# If your database is named something other than 'contractiq', update that as well.
DATABASE_URL=postgresql://postgres:root@localhost:5432/contractiq
